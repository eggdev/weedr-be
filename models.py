# from db_config import db


# class User(db.Document):
#     # id = db.StringField(required=True, unique=True)
#     name = db.StringField(required=True, unique=True)
#     # redditor = db.StringField(required=True, unique=True)
#     # reported_items = db.ListField(db.ClassField(), required=True, unique=True)
#     # reported_by_mod_count = db.NumberField(required=True, unique=True)
#     # reported_by_user_count = db.NumberField(required=True, unique=True)
